import React from 'react';

import classes from './Header.css';
const header = (props) => (
  <header>
    <div>Logo</div>
    <div>Navigation</div>
    <div>searchbar</div>
  </header>
);

export default header;